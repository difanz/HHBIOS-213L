[pytest]
testpaths = qa/spec
addopts = -ra --strict-markers --strict-config
xfail_strict = true
markers =
    unit: execute production 16-bit routines with guarded memory and a recording renderer
    dos: execute resident COM modules in DOSBox-X and inspect exported VGA state
    application: optional third-party application regression
